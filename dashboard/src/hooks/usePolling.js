// Polls GET /api/status/:taskId every 3s for each pending job, and stops
// once it's done or failed. Stubbed out until build-order step 4.

function usePolling(/* jobs, onUpdate */) {
  // no-op for now
}

export default usePolling;
